# Model configuration for chatbot integration

EMOTION_MODEL_PATH = "models/emotion_classifier_corrected"
TOPIC_MODEL_PATH = "models/topic_classifier"

# Add other config options as needed (e.g., device, batch size) 